// import { useEffect, useState } from "react";
// import { useParams } from "react-router-dom";

function FighterDetails() {
  // const { id } = useParams();
  // const [fighter, setFighter] = useState(null);

  // useEffect: fetch GET http://localhost:3000/api/fighters/:id
  // Browser route /fighters/2  →  API route /api/fighters/2  →  SQL WHERE id = $1

  // Bonus: delete button → DELETE /api/fighters/:id → navigate("/fighters")

  return (
    <main>
      <p>TODO: show fighter details for this id</p>
    </main>
  );
}

export default FighterDetails;
