// import { useState } from "react";
// import { useNavigate } from "react-router-dom";

function AddFighter() {
  // Class exercise: form for name, nickname, division, wins, losses
  // On submit → POST http://localhost:3000/api/fighters
  // Then navigate("/fighters")

  return (
    <main>
      <h1>Add Fighter</h1>
      {/* TODO: build the form + POST fetch */}
    </main>
  );
}

export default AddFighter;
